pipeline {
  agent any
  
  stages {
  
    stage("Build") {
      steps {
        echo "Starting the Build"
      }
    }
    
    stage("Test") {
      steps {
        echo "Starting the Test"
      }
    }
    
    stage("Deploy") {
      steps {
        echo "Starting the Deploy"
      }
    }
  
  }
}
